import React from 'react';

const SelectList = ({ title, url, handleChange }) => {
	return (
		<div>
			<label htmlFor=""></label>
			<select name="" id="">
				<option value="">---</option>
			</select>
		</div>
	);
};

export default SelectList;
